# jarmacz.com Environment Variables Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control

# Flask Configuration
SECRET_KEY=your-secret-key-here-generate-a-random-string
DEBUG=False
PORT=5000

# SMTP Email Configuration (Required for contact form)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
RECIPIENT_EMAIL=recipient@example.com

# CORS Configuration (Optional - defaults are set in app.py)
# ALLOWED_ORIGINS=https://jarmacz.com,https://www.jarmacz.com,http://localhost:5000

# Redis Configuration (Optional - for future caching/sessions)
# REDIS_URL=redis://localhost:6379/0

# Rate Limiting (Optional - uses in-memory by default)
# RATELIMIT_STORAGE_URL=redis://localhost:6379/1

# Deployment Environment
# Set to 'production', 'staging', or 'development'
# ENVIRONMENT=production

# ============================================
# SETUP INSTRUCTIONS
# ============================================
#
# 1. Copy this file: cp .env.example .env
# 2. Generate a secure SECRET_KEY:
#    python3 -c 'import secrets; print(secrets.token_hex(32))'
# 3. Set up Gmail App Password:
#    - Go to Google Account → Security → 2-Step Verification
#    - Scroll to "App passwords" → Generate new app password
#    - Use that password for SMTP_PASSWORD
# 4. Fill in your actual email addresses
# 5. Save the file
# 6. Verify .env is in .gitignore
#
# For production deployment:
# - Set DEBUG=False
# - Use strong SECRET_KEY (64+ characters)
# - Use app-specific passwords, never real passwords
# - Consider using environment-specific .env files
#
